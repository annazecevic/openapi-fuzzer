# .env.example
# Kopiraj u .env i prilagodi

# Putanja do OpenAPI spec fajla (unutar kontejnera)
FUZZER_SPEC=examples/bookstore.yaml

# Base URL target API-ja
FUZZER_URL=http://mock-api:8080

# Opcioni Bearer token
FUZZER_TOKEN=